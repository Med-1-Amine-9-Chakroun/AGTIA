import "../styles/navbar.css";

export default function NavBar() {
  return (
    <header>
      <div className="container">
        <div className="logo">
          <h1>AGTIA</h1>
        </div>

        <nav>
          {/* links for : */}
          {/* Mode button */}
          {/* notification icon */}
          {/* user icon */}
        </nav>
      </div>
    </header>
  );
}
